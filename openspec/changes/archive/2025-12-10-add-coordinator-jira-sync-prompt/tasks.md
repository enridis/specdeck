## 1. Implementation
- [x] 1.1 Add `specdeck-jira-sync.prompt.md` with metadata and coordinator Jira sync workflow steps
- [x] 1.2 Update `specdeck init copilot` and `specdeck upgrade copilot` to install/upgrade the new prompt and surface it via `--list`/`--template`
- [x] 1.3 Add coordinator-aware CLI commands for Jira sync planning (story/Jira pairs) and rich story detail (all fields, multi-ID, JSON)
- [x] 1.4 Write tests/fixtures for prompt packaging and new CLI commands (cache + overlay paths, JSON/table output)
